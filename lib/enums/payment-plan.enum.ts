export enum PaymentPlanType {
  single = 'single',
  multiple = 'multiple',
}

export enum PaymentPlanFrequency {
  daily = 'daily',
  weekly = 'weekly',
  monthly = 'monthly',
}

export enum PaymentPlanStatus {
  active = 'active',
  inactive = 'inactive',
}
