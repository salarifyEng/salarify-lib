export enum Currency {
  NGN = 'NGN',
  USD = 'USD',
  EUR = 'EUR',
  GBP = 'GBP',
}

export enum SupportedCountry {
  NG = 'NG',
}

export enum MailTypes {
  EmployeePayrollSuccess = 'employee_payroll_success',
}

export enum MFAType {
  TOTP = 'totp',
  Email = 'email',
  Phone = 'phone',
}
