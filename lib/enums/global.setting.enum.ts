export enum GlobalSettingType {
  GATEWAY_FEE = 'gateway_fee',
  SALARIFY_FEE = 'salarify_fee',
}

export enum GlobalConfigKey {
  BusinessAppConfig = 'business-app-config',
  PayrollFee = 'payroll-fee',
  PayoutChannel = 'payout-channel',
  FundingChannel = 'funding-channel',
  AccountResolveChannel = 'account-resolve-channel',
}
